# Définition des chemins
$DossierReception = "Z:\"  # Dossier réseau monté
$DossierTransfer = "C:\projet\disponible"
$FichierLog = "C:\projet\logs\logs.txt"

# Fonction d'écriture dans le log
function Write-Log {
    param ($Message)
    "$((Get-Date).ToString("yyyy-MM-dd HH:mm:ss")) | $Message" | Out-File -Append -FilePath $FichierLog
}

# Vérification de l'existence du lecteur réseau
if (!(Test-Path $DossierReception)) {
    Write-Log "❌ Le lecteur réseau $DossierReception est inaccessible. Vérifiez la connexion."
    Write-Host "Le lecteur réseau $DossierReception est inaccessible. Vérifiez la connexion."
    exit
} else {
    Write-Log "✅ Le lecteur réseau $DossierReception est accessible."
}

# Récupération des dossiers dans le répertoire de réception
Get-ChildItem -Path $DossierReception -Directory | ForEach-Object {
    $DossierUtilisateur = $_.FullName

    # Vérification que le dossier source existe
    if (Test-Path $DossierUtilisateur) {
        Write-Log "📂 Dossier détecté: $DossierUtilisateur"

        # Création du dossier de destination s'il n'existe pas
        $DossierTransfertUtilisateur = "$DossierTransfer\$($_.Name)"
        if (!(Test-Path $DossierTransfertUtilisateur)) {
            Write-Log "📂 Création du dossier de transfert: $DossierTransfertUtilisateur"
            New-Item -Path $DossierTransfertUtilisateur -ItemType Directory | Out-Null
        }

        # Déplacement du dossier vers le dossier de transfert
        Write-Log "🔄 Déplacement du dossier $DossierUtilisateur vers $DossierTransfertUtilisateur"
        Move-Item -Path $DossierUtilisateur -Destination $DossierTransfertUtilisateur -Force
        Write-Log "✅ Dossier déplacé: $DossierUtilisateur vers $DossierTransfertUtilisateur"
        Write-Host "Réussite: $DossierUtilisateur déplacé vers $DossierTransfertUtilisateur"
    } else {
        Write-Log "❌ Dossier source introuvable: $DossierUtilisateur"
        Write-Host "Dossier source introuvable: $DossierUtilisateur"
    }
}
